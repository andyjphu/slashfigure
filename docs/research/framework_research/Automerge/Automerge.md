# Automerge

- **URL:** https://github.com/automerge/automerge | **License:** MIT

JSON model CRDT in Rust with WASM bindings. Performance parity with Yjs (2.0). Compact binary encoding. Used by PushPin (2D canvas workspace) and PixelPusher (multi-user pixel editor). Proven for canvas collaboration.
