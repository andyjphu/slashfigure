# svg-path-commander

- **URL:** https://github.com/thednp/svg-path-commander (~289 stars) | **License:** MIT

DOM-free SVG path operations: `getBBox()`, `getTotalLength()`, `isPointInStroke()` from serialized path data. Critical for arrow/connector hit-testing and bounding-box computation without rendering.
